- name: 步骤 1：抓取 1.8M 原始生肉
        run: curl -L "https://raw.githubusercontent.com/Epodonios/v2ray-configs/main/All_Configs_Sub.txt" -o Meet/raw_meat.txt

      - name: 步骤 2：执行《镇邪卷》NodeJS 清洗
        run: node DataWashClean.js  # 这一步跑完，1.8M 的垃圾就变成了几百 KB 的精品 Candidate.json

      - name: 步骤 3：真机存活检测（基于 Candidate.json）
        run: |
          # 这里接入我刚才说的逻辑：不看名字，只看能不能连通。
          # 只有活下来的，才最后生成给平板用的 YAML。
